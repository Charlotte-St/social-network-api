const { ObjectId } = require('mongoose').Types;
const {User, Thought, Reaction} = require('../models');

module.exports = {
    //GET all thoughts
    //GET single thought
    // POST thought
    // PUT thought
    //DELETE thought
    //POST reaction
    //DELETE reaction
}